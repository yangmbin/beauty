<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/login_bg" >
    <LinearLayout 
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_centerInParent="true"
        android:layout_marginLeft="18dp"
        android:layout_marginRight="18dp">
        <EditText 
            android:id="@+id/login_edittext_top"
		    android:layout_width="match_parent"
		    android:layout_height="40dp"
		    android:background="@drawable/editbox_top"
		    android:hint="帐号"
		    android:textColorHint="#ffffff"
		    android:textColor="#aa000000"
		    android:textSize="16sp"/>
        <EditText 
            android:id="@+id/login_edittext_bottom"
		    android:layout_width="match_parent"
		    android:layout_height="40dp"
		    android:background="@drawable/editbox_bottom"
		    android:hint="密码"
			android:textColorHint="#ffffff"
			android:textColor="#aa000000"
			android:textSize="16sp"
			android:password="true"/>
        <Button 
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:text="登录"
            android:textSize="16sp"
            android:textColor="#aa000000"
            android:background="@drawable/btn_style"
            android:layout_marginTop="12dp"
            android:layout_marginLeft="4dp"
            android:layout_marginRight="4dp"
            android:onClick="loginToMainPage"/>
        <TextView 
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:text="注册帐号"
            android:textSize="12dp"
            android:textColor="#aaffffff"
            android:gravity="center"
            android:layout_marginLeft="200dp"
            android:onClick="registerTextviewBtn"
            android:clickable="true"/>
    </LinearLayout>
    
    <ProgressBar 
        android:id="@+id/login_progressbar"
        android:layout_centerInParent="true"
        android:visibility="gone"
        android:layout_width="45dp"
        android:layout_height="45dp"/>
</RelativeLayout>